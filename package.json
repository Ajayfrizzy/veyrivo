{
  "name": "veyrivo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@veyrivo/web",
    "build": "npm run build --workspace=@veyrivo/web",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run lint --workspace=@veyrivo/web",
    "typecheck": "npm run typecheck --workspace=@veyrivo/web",
    "test": "npm run test --workspace=@veyrivo/web",
    "db:generate": "npm run db:generate --workspace=@veyrivo/web",
    "db:migrate": "npm run db:migrate --workspace=@veyrivo/web",
    "db:seed": "npm run db:seed --workspace=@veyrivo/web"
  },
  "engines": {
    "node": ">=22"
  },
  "overrides": {
    "next@16.2.11": {
      "postcss": "8.5.12",
      "sharp": "0.35.0"
    }
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
