"use client";
import { Bell, CheckCheck, Settings2 } from "lucide-react";
import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
import { PageHeader } from "@/components/layout/page-header";
type Notification = { id: string; title: string; body: string; href?: string; readAt?: string; createdAt: string };
export function NotificationInbox() { const [items, setItems] = useState<Notification[]>([]); const load = useCallback(async () => { const response = await fetch("/api/notifications"); const body = await response.json(); if (response.ok) setItems(body.data); }, []); useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer); }, [load]); async function readAll() { await fetch("/api/notifications/read", { method: "POST" }); await load(); } return <><PageHeader eyebrow="Inbox" title="Notifications" description="Updates that require attention across jobs, payments, security, and support." icon={Bell} action={<div className="header-actions"><button className="secondary-button" onClick={readAll}><CheckCheck size={16} /> Mark all read</button><button className="icon-button bordered" aria-label="Notification settings"><Settings2 size={17} /></button></div>} /><section className="panel notifications-list">{items.length === 0 && <p className="support-empty">No notifications yet.</p>}{items.map(item => { const content = <><span className="notification-icon"><Bell size={17} /></span><div><strong>{item.title}</strong><p>{item.body}</p><small>{new Date(item.createdAt).toLocaleString()}</small></div>{!item.readAt && <span className="unread-dot" />}</>; return item.href ? <Link className={item.readAt ? "" : "unread"} href={item.href} key={item.id}>{content}</Link> : <article className={item.readAt ? "" : "unread"} key={item.id}>{content}</article>; })}</section></>; }
